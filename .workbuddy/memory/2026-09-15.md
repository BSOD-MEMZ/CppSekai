# 2026-09-15

## UI 组件动画（commit 2615a37）

- 用户要求「多加点动画，比如 combobox 的还有其它组件的动画」。做法是在 `game/Ui.cpp`
  顶部加两个小工具，取代每个组件各写一套：
  - `animValue(key, target, rate)`：按 ImGuiID 存在 `unordered_map` 里的一个值，
    每帧 `v += (target - v) * (1 - exp(-rate * dt))` —— 指数逼近，步长由 DeltaTime
    推出（帧率无关），且**永远不会过冲**（比弹簧稳，不需要额外阻尼参数）。
    差值 < 0.0015 直接吸附，免得为了 0.1% 的变化一直重绘。
  - `mixColor(a, b, t)`：用 ImGui 的 ColorConvert 做 RGBA 插值。
- 接进去的地方：tabBar（选中页签从下面滑上来 + 填色/文字色渐变 + 悬停提亮）、
  slider（`##thumb` 悬停/拖动时放大 16%，± 按钮悬停变亮、按下变暗都用插值）、
  capsuleButton（悬停放大 2%、按下缩 3%，颜色三层混）、checkBox（对勾从盒子中心
  `scale 0.5→1` 长出来 + 淡入，边框同时淡出）、stepper（胶囊悬停/按压插值）。
- 顺序坑：slider 的把手原来画在 `InvisibleButton` **之前**，拿不到 hover 状态 —— 把
  绘制挪到 ± 按钮之后，用 `ImGui::GetID("##thumb")`（不是 `GetItemID()`，那时它已经是
  最后一个按钮的 id）当 key。
- 新增 `ui::combo()`：包一层 `BeginCombo`。
  - 淡入：ImGui 每帧重建 popup，所以"渐变"全靠 `PushStyleVar(ImGuiStyleVar_Alpha, …)`
    包住 BeginCombo，配合 popup 打开状态（`IsPopupOpen("##combo", …)`）驱动的 0→1。
  - 旋转箭头：`ImGuiComboFlags_NoArrowButton` 关掉内置箭头（它画在控件内部，动不了），
    自己在 `GetItemRectMin/Max()` 右端画一个三角形，按 `t * π` 绕中心旋转。
  - 最后一个参数 `scaleHint`：选曲界面用的是自己的 `k = h/1080`，和 `ui::scale()`
    （h/860）不是一回事，不传的话箭头大小会不匹配。
- 验证：`--screenshot`（选曲）+ `--settings --settings-tab 1`（设置）两张静态图都正常
  （像素统计：几万种颜色、亮度分布正常），动画本身没做逐帧回归。

## 下载器歌曲表去重（同一个 commit）

- 用户报「搜索チーム出现两个 id 一模一样的チームメイト」。查下来**本地数据里没有重复**：
  `musics.json` 715 条 id 唯一、仓库根和 build/ 两份 md5 相同、`chartdl --list` 全表
  只有一条 `0374 チームメイト`。`--list <日文>` 也没法用来复现 —— exe 走窄 argv（ACP），
  日文过滤器传进去是乱码（**踩坑：验证日文搜索别用 `--list`，只能用 GUI**）。
- 结论：重复只可能来自他用过的那份数据/那个 exe。按需求做了防御性去重，并且**让它在日志里
  可见**，而不是悄悄吞掉：`loadData()` 用 `std::set<int>` 丢掉后来重复的 id，
  `rebuildList()` 再用一个 set 兜一层（哪怕 gSongs 被别处污染），丢弃条数打印成
  `[data] dropped N duplicate song id(s)`。理由写在注释里：输出文件名 `0374_normal.sus`
  完全是 id 拼的，同 id 两条记录既重复列行、又会让两个下载任务抢同一个输出路径。

## 下载器绑 Common Controls v6（同一个 commit）

- 用户说的「Windows 那个什么什么组件库 6.0 的系统存根」= `Microsoft.Windows.Common-Controls`
  6.0 的 **SxS 程序集依赖**（通过应用清单声明）。
- 做法：新增 `app.manifest`（`<dependency>` 指 v6 + `asInvoker`），`app.rc` 加一行
  `1 24 "app.manifest"`（RT_MANIFEST = 24，CREATEPROCESS_MANIFEST_RESOURCE_ID = 1）。
  `zig rc` 直接支持 `ID TYPE "外部文件"` 这种写法，不用内联。游戏 exe 和下载器共用
  `build/app.res`，所以两个都带上了（游戏不用原生控件，无副作用）。
- **没有加 DPI 声明**：`dpiAware` 会改变窗口的实际尺寸（高缩放屏上界面变小），用户没要求，
  先不动。
- 验证方法（可复用）：起一个实例，PowerShell 看进程模块
  `Get-Process chartdl | % { $_.Modules | ? ModuleName -eq 'comctl32.dll' } | % FileName`，
  结果落在 `C:\Windows\WinSxS\amd64_microsoft.windows.common-controls_6595b64144ccf1df_6.0.19041.6456_none_...\COMCTL32.dll`
  就是 v6；绑到 `C:\Windows\System32\comctl32.dll` 就是没生效。
  **注意 `FileVersionInfo.FileVersion` 显示的是 5.82**（那是 MUI 资源的旧号），别拿它当判据。

## 杂记

- 编译时 `lld-link: failed to write output 'build\cppsekai.exe': Permission denied` ——
  是用户自己开着的实例占着 exe。**用户已明确授权：以后再遇到占用直接杀**
  （`Stop-Process -Name cppsekai,chartdl -Force`）。
- 提交范围：这次只 add 了明确的 7 个文件。`assets/` 下仍有 12 项（约 380 个文件，
  字体 / overlay_opt / effects / sound / notes.png / select/indicate_back.png 等）
  **从未入库**，虽然在 `.gitignore` 注释里写着「Tracked in full since 2026-09-12」，
  但一直没提交过。等用户决定要不要补一次（补的话仓库会多 20MB 左右）。

## 第二轮：缓动铺到选曲界面自绘部件（commit 2bca803）

- 把 `ui::anim` / `ui::mix` 暴露到 `Ui.hpp`（内部就是 `animToggle` / `mixColor`）。
  **它们的 id 空间是本模块私有的**，所以调用方不必凑 ImGui 的 ID 栈，直接用常量
  `0x4a55x000u + index` 当键即可；我按用途分了段（…2=列表行、…1=分组标题、…3=跳转字母），
  这样"换到另一行时旧行还能淡出"，而不是全局共用一个 t。
- 接入点：列表行 hover 高亮、分组标题条（底板/左侧标记条/文字/箭头共用同一个 t）、
  跳转面板字母（字号 +6%、颜色渐白，当前分段恒亮）、右下角随机/设置圆钮（圆盘+图标
  一起放大 6%、底色插值到亮紫）。
- 验证：静态截图（hover 根本没触发）与改动前**逐项一致**（mean 101.2/107.2/127.9、
  9210 种颜色）—— 这是"确认没画崩"的快速手段；动画手感本身没法无头回归。
- 踩坑：**编译期间改源码会失败**，报 `UNKNOWN: unknown error, open 'xxx.cpp'` ——
  zig 编译时占着源文件。要么等它编完，要么先改完再编（别一边 `run_in_background`
  编译一边 Edit）。

## 第三轮：页签 / 换歌 / 列表重建过渡（commit fdff465）

- **设置卡片换页签**：新页内容从下方 14px 滑入（0.18s，ease-out）。改动只有一句话
  ——把 child 内初始 `SetCursorScreenPos` 的 y 加上 `tabSlide`。**这里刻意用位移而不是
  淡入**：页签内容里既有 ImGui 原生控件又有 `ui::` 手绘部件，前者认
  `ImGuiStyleVar_Alpha`、后者不认，混着淡入会一半淡一半不淡；位移对所有部件一致。
- **手机面板换歌**：内容块（封套/元数据/难度圆/徽章）上浮 16px + 淡入，手机框不动。
  复用了文件末尾那个"把所有 phone 顶点做 tilt"的 pass：在内容开始处记
  `phoneContentVtxFirst`，pass 里用 `i >= phoneContentVtxFirst` 区分内容与边框，内容额外
  加 y 偏移、alpha 与入场动画取 min。
  **这个套路值得记**（顶点范围 + 事后一次 pass）：想让某块绘制整体动起来时，先记
  `dl->VtxBuffer.Size` 当分界，事后再统一改 pos / col —— 不用碰那块绘制的任何一行代码。
- **列表重建**：`listSignature` 变化时 `listIn = 0`（新列表上浮 12px + 淡入），与
  section index 的淡出共用同一个顶点 pass（原来那个 pass 只在 `indexAnim > 0` 时跑）。
- **验证踩坑（重要）**：用 `--screenshot` 默认的 1.2s 做"静止态无回归"对比**会误判** ——
  选曲界面 mean 从 101.2 掉到 93.8，看着像改坏了，其实只是截图模式帧数少、动画没跑完
  （`frameDt = clamp(io.DeltaTime, 0.0005, 0.05)`，截图模式一帧最多算 0.05s）。
  把 `--screenshot-time` 给到 2.5 / 4 之后与改动前逐项一致（101.2/107.2/127.9）。
  **以后凡是对比静止态，时间给足再比。**

## 第四轮：界面缩放 + 下载器字体/列/排序 + DPI（commit d967133）

- **界面缩放**（用户要求，只给选曲 + 结算，打歌界面明确排除）：`UserSettings::uiScale`
  0.7~1.5，存 `userdata.json`；CLI `--ui-scale`，设置卡片「画面」页滑杆。
  - 选曲：原来是 `k = clamp(h/1080)`，现在拆成 `kBase`（窗口大小那份）+ `k = kBase * scale`；
    **手机面板必须额外乘 scale** —— 它是唯一用窗口尺寸算出来的部件（`phoneH = (h - 24kBase) * scale`），
    不乘的话列表变大、面板不动，面板内部按 k 缩放的文字会溢出手机框。
  - 结算：`makeCanvas(dl, w, h, uiScale)` 的 scale 乘上去。**`resultContinueHitTest` 必须传同一个
    值**，否则「继续」按钮判定框和画面对不上 —— 这是最容易被漏的一处（有默认参数，漏了不报错）。
  - 验证：`--ui-scale 0.75/1.3` 与 1.0 的截图对比 mean diff 15.4 / 22.3 ✓。
- **下载器**：
  - 字体 `createUiFont()`：依次试 Microsoft YaHei UI → Microsoft YaHei → Segoe UI → Tahoma，
    每次 `SelectObject` + `GetTextFaceW` 反查**确认没被 GDI 替换**（CreateFontW 永远成功，会静默替换）。
    实测拿到 `Microsoft YaHei UI` ✓（`[ui]` 日志里能看到）。
  - 五个难度各一列 + 点表头排序（再点反向）。**坑**：排序标记必须写给**表头控件**
    （`Header_SetItem` + `HDF_SORTUP/DOWN`）—— 只写 ListView 的列（`LVM_SETCOLUMN`）时
    `LVM_GETCOLUMN` 读回来是新的，但屏幕上的表头**完全不动**（ListView 和表头各存一份文本）。
    我在这上面绕了三轮：先怀疑没重绘（加 RedrawWindow 无效），再怀疑列太窄被裁（换成宽列仍 0 差异），
    最后才想到 v6 的 ListView 不往表头同步。
  - **无头验证排序**：给 winmsg 加了 `clickchild <childClass> <x> <y>`（表头是独立子窗口，
    EnumWindows 看不到，原来的 `click` 点不到）。点 EXPERT 列头后列表行区域 diff 4.74、
    changed bbox 正好是行区域 ✓ 排序确实生效。表头那 24px 里的标记**没能从像素上确认**
    （我把表头带渲染成字符图也没解读出结论），留给用户肉眼确认。
  - `--screenshot` 抓图前加 `RedrawWindow(RDW_ALLCHILDREN)`：否则被 `WM_SETREDRAW FALSE`
    冻住的子控件会被拍成旧的。
- **DPI（用户问的）**：
  - 结论：**两个程序原本都不是 DPI 感知**。SDL2 的 `SDL_HINT_WINDOWS_DPI_AWARENESS` 默认是空串
    （= 不改变感知，SDL_hints.h 里写明），main.cpp 没设；chartdl 原来也没有 manifest 声明。
    未感知 = 在高缩放屏上被系统位图拉伸（糊），`SDL_WINDOW_ALLOW_HIGHDPI` 因此没有实际作用。
  - **别信 `HKCU\Control Panel\Desktop\LogPixels`**：这台机器它写着 192，但 `Win8DpiScaling=0`
    且实测 `GetSystemMetrics(SM_CXSCREEN)=1440x900` = 物理分辨率、`GetDeviceCaps(LOGPIXELSY)=96`
    → **实际是 100%**，那个 192 是过期残留。我一开始据此说"你是 200% 缩放"，错了，要更正。
  - 现在：chartdl 声明了系统 DPI 感知（`chartdl.manifest`），布局全部过 `dp()`，`--dpi <n>`
    可强制缩放做验证；**游戏故意没动**（要同时动 manifest 和 SDL hint，且窗口物理尺寸语义会变）。
  - 资源分家：`resources.rc`（图标 + 版本）+ `app.rc` / `chartdl.rc`（各自清单）——
    两个 exe 共用一份 `.res` 就没法让下载器单独感知 DPI。

## 第五轮：把"表头标记没确认"这个尾巴收掉（commit 0794a1d）

- 上一轮留了一句"表头那个 ▲ 我没能从像素上确认"。结论是**它一直是好的，我测错了地方**。
- 根因：`--screenshot` 里 `PrintWindow(hwnd, dc, PW_RENDERFULLCONTENT)` **漏了 `PW_CLIENTONLY`**。
  位图是按**客户区**尺寸开的，但 PrintWindow 把**整窗（含标题栏）**渲染进去 → 图顶上 ~30px 是
  标题栏、客户区底部被截掉，**图上每个 y 都比客户区坐标偏 ~30px**。我按客户区坐标去找表头，
  实际量到的是"搜索框那一行"（那一带正好有搜索框/按钮的边框线，看着还像表头，所以没起疑）。
  → 记住：**这个工具的截图坐标 ≈ 客户区坐标 + 30**（修正前），出图后先确认基准再量。
- 修正后（`PW_CLIENTONLY | PW_RENDERFULLCONTENT`）表头文字正好落在 y 76..88 = 客户端 70..90 ✓
  与 WM_SIZE 的 dp(68) 对上了。
- **量化确认标记**（按列统计表头墨迹，比看图可靠）：
  初始 ID 排序时 `ID=86 / EXPERT=138`；点 EXPERT 后 `ID=39 / EXPERT=151`
  —— 标记从 ID 列移到了 EXPERT 列 ✓。以后验证这类"文字/标记有没有画"就用这个方法。
- 教训：**先确认测量基准，再下结论**。这次两轮"没重绘""列太窄被裁"的推理全是错的，
  浪费在于没有一开始就把整图粗看成字符画定位布局。

## 第六轮：六项（commit e04a027）

用户一次给了六件事，全部做完并逐项做了量化验证。

- **表头 ▲/▼ 文字标记去掉**（用户不要），只留 `HDF_SORTUP/DOWN` 原生箭头 —— 顺便说明：
  上一轮那个"表头标记"结论仍然成立，只是现在不再改列文本，只改格式位。
- **初始血量**：`UserSettings::initialLife`（100..1000）。**坑**：引擎里 `reset()` 和 `load()`
  **都会** `mStats = JudgementStats{}`，而一次开局是先 reset() 再 load()，所以两处都得种
  `mStats.life = mInitialLife` —— 只改 reset() 会被 load() 覆盖回 1000（我实测踩了一次：
  `[settings] setting=300 engine=300` 但 `[stats] life=1000`）。验证：autoplay 跑 12s，
  300 -> `life=300 (30.0%)`、1000 -> `100.0%` ✓
  验证时的另一个坑：**`--screenshot` 默认停在选曲界面**（`[stats]` 打的是默认结构体，
  永远是 1000），要真进演奏得给 `--sus <谱面>`。
- **设置卡片滚动**：画面页实测 `used=866 / view=642 / scrollMax=220` → 之前那 224px 是被
  child 裁掉的（就是用户说的"挤不下"）。两处要点：去掉 `NoScrollbar`；child 内首行改用
  **窗口内坐标** `SetCursorPos`（原来是 `SetCursorScreenPos(绝对 y)`，滚动时第一行会被钉住）。
  诊断日志 `CPSEKAI_UI_TRACE=1` → `[ui] tab N view= used= scrollMax=`。
- **combo 半透明**：根因是我自己上一轮引入的 —— `PushStyleVar(Alpha, 0.3+0.7t)` 包住了整个
  `BeginCombo`，而**关闭时 t 恒为 0**，于是下拉框本体一直是 0.3 透明度。修法：alpha 完全不
  参与，改成弹窗滑入（`SetWindowPos` 每帧从 ImGui 重设的锚点重算，不会漂移）+ 自绘箭头旋转。
  采样验证：combo 内部 (61,56,94) = 不透明底色（旧的半透明会是 ~(37,33,67)）。
- **确定 → 白光转场**：点确定**不立刻** startSession，先跑白光 0.28s 扩散 + 0.16s 全白 +
  0.55s 褪去，**在白屏铺满那一刻才加载**，把谱面/音频/**舞台底板生成（实测 1052ms）**全挡在
  白屏后；时间轴用 `min(frameDelta,0.05)` 累加，否则卡顿那帧会把褪色跳过去。录制用的坐标来自
  `drawSongSelect` 的新出参 `confirmCenter`（**倾斜后**的中心，1920x1080 下 = 1623,783）。
  验证：点击后截图 → 按钮处亮度 255、中距 181、远角 112（径向扩散 ✓）；再跑一次确认转场后
  正常进演奏（`[stats] score=127442` ✓）。
- **曲末渐暗 → 结算**：`effectiveEnd` 前 1.2s 渐黑，结算页 0.6s 抬起。实测
  7.4s→33.8、7.95s→**2.9（纯黑）**、8.6s→80.4（结算页）。让 `--result-at` 也走同一条终点，
  这样这条链子可以用 `--result-at` 快速验证（本来要跑完整首歌）。
- **结算底板透出**：上层自绘 wash 的 alpha 从 `appear*255` 降到 `*0.70`，让 `renderFrame`
  画的舞台底板透出来（用户要求"上层背景增大透明度"）。
- 工具/流程备注：`winsend.exe` 没构建过，用 `winmsg.exe SDL_app click x y` 代替（SDL 窗口
  类名就是 `SDL_app`）；新加的 `[ui] confirm button at x,y` 诊断就是为了不再手算坐标
  （我第一次手算错了 78px）。**点确定要等 `[boot] ready` 之后再 sleep 对齐截图时刻**，
  因为 `--screenshot-time` 是从主循环开始算的，而 boot 要 ~2.2s。

## UI 音效 + 发布包自带素材（已 commit 75590ad）
- 需求：点组件播 `assets/se/click.mp3`、弹窗开/关播 window_open / window_close、
  难度按钮播 level_choose、选曲每滑一格播 select；**会弹窗的按下别播 click**
  （window_open.mp3 里混了 click，同帧会变成双击）。
- 做法：`AudioEngine::UiSe`（5 种 × 3 声部，`loadSe` 末尾 `loadUiSe`，缺文件只打日志）
  + `ui::se()` 记请求 + 帧尾 `ui::flushSe()` 播**本帧最高优先级**那一个
  （click<select<level_choose<window_open<window_close）。优先级顺序就是"不播 click"的实现。
  开关窗音在 `beginCard` 里按 `CardState::soundOpen` 触发，`requestClose()` 负责同帧响+不重复。
- 接线点：Ui.cpp 全部组件、HUD 暂停键、SongSelect 的滚轮/方向键/落点/分区字母/chip/难度/确定/
  随机/设置/重扫（拖拽连续滚动只在落点响，否则 fling 变成噪音）。
- 打包：`package.sh` 默认带 `assets/`（66MB，zip 51MB，解压即玩）；`--no-assets` 保留旧精简包。
- 坑：`build.sh` 偶尔 `failed to write output build\cppsekai.exe: Permission denied`——
  没有残留进程，重跑一次就好（杀毒/文件锁抖动）。

## 第二轮：结算配色 / 音量 / 光效 / 手柄（commit 1b07668）
- 结算难度色：难度胶囊 + 曲绘边框都是写死的 `kPink`（参考截图是 EXPERT 的）→ 改用新公开的
  `game::difficultyColor()`。验证手法：`--sus charts/0001_easy.sus --result-preview
  --screenshot x.png` 然后 ffmpeg 转 raw 采样像素（**没有 ImageMagick，用 ffmpeg -f rawvideo
  -pix_fmt rgb24 更稳**）= (75,207,138) 绿 ✓。
- 音量：`UserSettings::bgmVolume` + `AudioEngine::setBgmVolume`（引擎主音量，谱面/试听/结算
  BGM 一起缩，必须在第一次 loadMusic 前设）；SE 沿用 seVolume，滑杆里重新 `ui::bindSe`。
  验证：统计设置卡里的薄荷轨道 = 4 条（166/240/314/388）。
- 确定键光效：峰值 0.88、射线顶点级渐变（1.92 无 AddTriangleFilledMultiColor → 手写
  PrimReserve/PrimWriteVtx/PrimWriteIdx）、全屏圆 = 16 层同心圆叠加。新加 `--confirm-flash`
  调试开关才拍得到这个过渡（原来只能靠真点击，PostMessage 的 winmsg click 在没焦点的会话里
  没送进去）。
- 手柄：SDL_INIT_GAMECONTROLLER + `SDL_PushEvent` 合成键盘事件 → 复用手柄无关的
  方向键/Enter/Escape 逻辑。**一次按压必须拆两帧（本帧 DOWN / 下帧 UP）**，同帧 down+up
  ImGui 不一定认。本机确实插着 Xbox 360 手柄，日志 `[pad] Xbox 360 Controller connected`。
  合成按键这条路用临时 self-test 验证过（能打开设置卡），真手柄按键映射待人工确认。
- 顺手：选曲界面键盘 ←→ 换难度现在也播 level_choose（之前只有鼠标点播）。

## 第三轮：账户系统 + 玩家等级（本次）
- 需求：初步账户（昵称/学校等，**平时不显示**）+ 选歌 / 结算展示等级，等级图标 `assets/select/level.png`。
- 查证：Sekaipedia「Player Rank」——经验 = `m_score × m_bonus`；评级倍率 S320/A280/B240/C200/D20；
  升级所需经验的官方表：1 级 10、2 级 8010、3~12 级 +500/级、13~15 级 +1000/级、16 级起 +480/级，
  上限 900。（搜索只有 sekaipedia 打得开，fandom / wikiwand 的 fetch 全失败。）
- 落地：`game::AccountData`（存 userdata.json 的 `account` 段）+ `expToNextRank()` / `scoreRankExp()` /
  `addPlayerExp()`；经验挂在主循环记成绩那块，autoplay 和提前切结算都不加。
  `ui::playerLevelChip()` / `ui::expBar()` 两处复用（选曲右上、结算面板右下）；
  点选曲的等级牌弹**个人资料卡**（昵称/学校/签名/等级/经验/游玩次数）。
- 无头检查：新增 `--profile`（开资料卡）、`--player 昵称:组织`、`--player-rank N`（后两个只改内存）。
  验证：真跑完一首 0075_easy（失败，D 级）→ 日志 `[rank] d +20 exp -> rank 2 (10/8010) (rank up)` ✓
- **踩坑**：① `--sus` 的相对路径在 Git Bash 下读不到（`charts/x.sus` 和 `../charts/x.sus` 都失败，
  绝对路径正常，疑似 MSYS 改写参数）；② `--screenshot` 收的是**文件**不是目录，指到目录上静默不写；
  ③ 测试完记得把 userdata.json 还原（跑了首歌会写进去一条 failed 记录）。
- 收尾（commit e2b98eb）：`printUsage()` 补上 `--settings`（原来只有 --settings-tab）/
  `--profile` / `--player` / `--player-rank`；`ui::expBar` 高度 8→10、结算 EXP 文字
  21→23（1280x720 下只有 14px）。新页签把设置卡原来的 `else` 改成 `else if (tab == 3)`，
  复检「系统」页正常。
