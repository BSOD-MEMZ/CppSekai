# 2026-09-20

## chartdl 数据源检查 + 国服曲下载修复

**用户报的两个症状**
1. 搜不到国服的「Hype Dive」
2. 下载国服歌曲只能拿到曲绘 + BGM，谱面一个都下不到

### 根因

**搜索**：`matchesFilter` 是裸字节比较 → `hype` 匹配不上 `Hype Dive`（必须照抄表里的大小写）。
顺带发现命令行参数的坑：GUI 子系统的 `argv` 是 **ANSI 代码页**解出来的，
`chartdl.exe --list 镜中少女` 到手就是乱码，中文/日文过滤在 CLI 上从来没生效过。

**谱面下不到**：`updateDetailPanel` 用 `available = level > 0` 判断"这首有没有这一档"，
而定数表 `music-levels.json` 停在 id 804（712 首），**17 首国服独占曲一行都没有** →
5 个难度框全灰 + 取消勾选 → 点下载只排到曲绘和 BGM。
用 09-20 打包版复现确认：五档全是 `easy （无）` + 按钮 `OFF`。

资源侧本身没问题（都实测过）：
- 谱面 `storage.sekai.best/sekai-cn-assets/music/music_score/11017_01/master.txt`（**带 .txt**）
- 音频 `.../music/long/vs_11017_01/vs_11017_01.mp3`（`se_` 前缀没有）
- 曲绘 `.../music/jacket/jacket_s_11017/jacket_s_11017.png`

### 改动

**`downloader/chartdl.cpp`**
- 新增 `foldCase()`：搜索查询 + 标题都折 ASCII 大小写，别名表加载也用它。
- 新增 `utf8Args()`（`CommandLineToArgvW` + `toUtf8`），`main()` 解析循环改读 `args`。
- `Song::levelsKnown`：定数表里**没有这首歌** → 判为"未知"，难度框照常可勾（`!levelsKnown || level > 0`）；
  定数仍显示 `-`。启动日志新增 `[data] N song(s) have no levels in music-levels.json`（CLI + GUI 两处）。
- `queueSong` 的 `add()` 按路径去重（一个文件只排一个任务）；
  面板里同一个 `kIdVocalBase + v` 的重复行用 `GetDlgItem` 守卫挡掉。

**`.workbuddy/tools/update_music_db.py`**
- 同步范围加上 `music-levels.json`（日服 `musicDifficulties` 管 id<10000，国服那份管独占号段，
  只保留 `musics.json` 里真有的 id）。`write_levels()` 按"一行一首、id 升序"写，diff 才只有新增行。
- 每个仓库补 **jsDelivr 镜像**（`cdn.jsdelivr.net/gh/...@main`）：2026-09-20 实测
  raw.githubusercontent 直接 000 超时，只留 raw 的话脚本跑不起来。

**数据**：`music-levels.json` 712 → 733 首（+21：241 / 290 / 809 敗走 / 811 ヘレディティ + 国服 17 首），
变化 0、删除 0。`musics.json` / `music-vocals.json` 写回后逐字节没变（证明 writer 是忠实的）。

### 验证

- `--list hype` / `--list HYPE DIVE` / `--list 镜中少女` / `--list はいぷ` / `--list imasugurinne`
  （罗马音）/ `--list tyw`（别名）全部命中。
- 新工具 `.workbuddy/tools/chartdl_detail_check.py`：起 `--select <row>` 后跨进程读回
  `GetWindowTextW/IsWindowEnabled/BM_GETCHECK`（三个都不带指针参数，所以能跨进程用），
  比截图更适合验证"哪些框能勾"。Hype Dive（row 735）：五档 `ON check=1` + 真实定数
  `Lv.7/12/16/24/29`；13 版本曲（585，row 543）13 行 id 1120–1132 齐全。
- 下载：11017 → 7 文件全 ok（5 谱面 + 曲绘 + BGM）；374（日服）→ 3 文件 ok；
  585 多版本 → 14 文件、无重名。

### 没查出来的

「下载内容」面板**重复建同一个演唱版本勾选框**（09-20 打包版同样有，属既有问题，不是这次改出来的）。
探针显示：一次 `updateDetailPanel` 里难度循环跑了 **10** 遍（两轮 5 遍）、演唱版本循环跑了 2 遍，
但函数入口探针只打了 1 次 —— 行为像"从函数中间又跑了一遍"。成因没定，
已从两头堵死（面板守卫 + 队列去重）。工具留在 `.workbuddy/tools/chartdl_detail_check.py`。
