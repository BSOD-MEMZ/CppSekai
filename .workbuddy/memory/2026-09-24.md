# 2026-09-24

## 设置卡片一批改动（用户一次提的 5 件事，全部落地 + 验证）

1. **账户页：导出 / 导入用户数据**
   - 原生 comdlg32 选择器（`GetSaveFileNameW` / `GetOpenFileNameW`），**build.sh 游戏链接行
     新加 `-lcomdlg32`**；`OFN_NOCHANGEDIR` 必须留（否则挑完目录 cwd 被改，`charts\` / 日志 /
     相对 `--sus` 全错）。
   - 导出 = `persistUserData()`（先刷活镜像）+ `game::saveUserData(挑的路径, ...)`，默认文件名
     `CppSekai-<昵称>-<yyyymmdd>.json`（非法文件名字符换 `_`）。
   - 导入 = 新加的 `game::isUserDataFile()` 先筛（**在弹确认框之前**）→ `ui::eulaDialog` 二次确认
     → `std::filesystem::copy_file` **覆盖 profile 文件本身**（不 re-serialize，保留未知键）
     → 重读 + `applyProfileLive()` + `++profileDataGeneration`。
   - 为此把 `activateProfile()` 里「重新推活镜像」那 55 行抽成 `applyProfileLive`（用
     `std::function` 前置声明，因为它在 activateProfile 下面才定义）。`CPSEKAI_IMPORTASK=1`
     可无头弹那张确认框。
2. **系统页：结束当前实例 / 结束所有实例**
   - 全部实例 = EnumWindows 找类名 `SDL_app` + 标题 `CppSekai` / `CppSekai - <user>` 的窗口，
     `PostMessageW(WM_CLOSE)`（不 kill）。main 里**新加 `SDL_WINDOWEVENT_CLOSE` 分支**
     置 `running = false`（以前只处理 SDL_QUIT）。二次确认 `killAllFrames`。
3. **判定页：判定预设 宽松 / 标准 / 严格（radio）**，出厂**宽松**（perfect 70 / great 120 /
   good 170 / bad 230 / miss 230 = 官方各 +30ms）；`UserSettings` / `JudgementWindows` 的默认值
   一起改成宽松。拖滑杆 → 选中态退成 -1。长条容错也从 pick-one stepper 换成 radio row。
   `game/Ui` 新增 `ui::radioRow`（圆点 + 标签，一行平分宽度，走焦点环 PadKStepper）。
4. **震动三勾 + 自动演出 挪到演奏页**（分别从判定、画面）。
5. **触摸屏滚页签**：SDL_FINGER* 攒竖直位移 → `##tabcontent` 里 `SetScrollY`；方向只判一次
   （12*scale 的 slop，|dy|>|dx| 才算翻页），hover 用 `AllowWhenBlockedByActiveItem`。
   `SDL_FINGERUP` **不清**累积量（快甩的 down/move/up 同帧到达，清了就整段丢）。
6. 顺手修：判定页工作副本原来只在「卡片刚打开那一帧」刷新 → 卡片开在演奏页时切到判定页永远
   不刷新；现在按「这一页第一帧」刷（`judgePageLastFrame`），导入后也自然重读。
7. 设置卡片 780 → **800** 高（演奏页加了两块，515/519 顶到边）；账户 488、演奏 515、
   判定 635（**故意滚动**）。

## 验证（无头）
- 0 warning / 0 error（全量 `build.sh`）。
- 各页截图在 `build/shots/`：`g0/g1/g2/g3/g4`、`f_import.png`（导入确认框）、
  `f3.png`（结束实例按钮）、`f0/f4`。
- **全新档案**（临时目录跑）：`[settings] windows perfect=70 …` → 出厂宽松，判定页 宽松 选中。
- **触摸滚动**：临时探针往队列塞 `SDL_MOUSEMOTION(which=SDL_TOUCH_MOUSEID)` + DOWN + 12×MOTION，
  两张截图做配准 → 内容位移 **129px**（= scrollMax，请求 345px 被 clamp）。探针已删。
  ⚠ 事件**必须带 `windowID`**，否则 ImGui 后端直接丢（`GetViewportForWindowID`）。
- **结束所有实例的机制**：python ctypes EnumWindows 确认类名就是 `SDL_app`、标题 `CppSekai`，
  PostMessage(WM_CLOSE) 后日志出现 `[instance] window close -> exiting`、进程退出、
  profile 完整写回（43 个键）。
- **没验到的**：两个文件选择器本身（模态框要人手点一次），见下。

## 第二批（同一天，5 件事）

8. **窗口标题跟随用户**：`syncWindowTitle()`（SDL_CreateWindow 之后定义）在 `activateProfile()`
   和账户页改昵称两处调用。标题 = `CppSekai`，或房间标签存在时 `CppSekai - <玩家名>`
   （`--party-name` 优先）。**单人窗口必须保持裸 `CppSekai`** —— 单实例的前台查找按它精确匹配。
9. **开多人游玩弹「立即重启？」**：`drawRestartAskDialog` + `restartSelf()`（GetCommandLineW →
   CreateProcessW 重开自己）。⚠ **先 CloseHandle singleInstanceLock / profileLock**，否则子进程
   读到"已有实例"会把老窗口叫到前台再自杀，重启完一个窗口不剩。
10. **自动演出给经验**：照官方 AUTO LIVE —— 给经验 / 场次 / 总分，**不写 cleared / FC / 最高分**，
    结算强制 `newRecord = false`；`--auto`（命令行）连经验都不给。
    顺手修：读旧最高分从 `scores[key]` 改成 `scores.find(key)`（下标会凭空建空记录）。
11. **README 581 → 203 行**：原理详解（tick 三轴 / 假透视 / 填充检测参数）搬 AGENTS.md，
    chartdl 用法搬 CLI.md，打包发布搬 SETUP.md。技术细节一律别往 README 加。
12. **MD 图片引用审计**：`docs/preview*.png` 里被引用的只有 5 张
    （playonseewo.jpg / preview.png / preview_group_reading / preview_result / preview_downloader）；
    **未被任何 md 精确引用**：preview_auto / preview_pause / preview_select / preview_settings /
    preview_stage / preview_vocal（COPYRIGHT.md 与 CREDITS.md 只写了通配 `docs/preview*.png`）。
    → 用户拍板 **已 `git rm` 删除**（commit 8acbdbb），docs/ 只剩那 5 张，逐张复核都有引用。
13. **「踏步、出发」（id 11003，国服独占）红谱最后一条 hold 响个不停**：查谱面文件，
    `11003_expert.sus` 的 `1x`（长条）总数是 **224 = 奇数**，最后一条 `#037:17 lane1` 之后
    只剩两个 flick —— **谱面最后那条长条只有起点没有结束标记**（master 同样，281 个）。
    → 结论：**谱面本身的缺陷**（国服那套数据搬运时被截尾），不是引擎算错。
    但引擎缺兜底：结算/离开演奏时应强制松手并掐掉 tick 音，或 load 时把没结束标记的长条
    按最后 tick 收尾。**用户暂未要求修，未动代码。**

### 这次踩到的验证坑（值得记住）
- **`--sus` 单跑根本进不了 Play**：窗口一失焦 + `autoPauseOnBlur`（默认开）→ 前摇期间
  (`songTime < 0`) 直接退回选歌、停音乐。无头验证要在档案里写 `"autoPauseOnBlur": false`。
- **`--result-at` 不会记成绩**：那条门是 `songTime >= trackDurationSec - 0.25`（真实曲长），
  所以要用 `--bgm <短音频>` + `--filler 0` 跑完整局才能验经验结算；
  合成的短 wav 用 python `wave`，7 秒正弦就够（纯音没有静音头，filler 自然是 0）。
- 用户游戏/下载器开着时 `build.sh` 会 `Permission denied`：用 `OUT=build/probe.exe` 编，
  运行时要加 `--instance multi`（否则被单实例 mutex 挡掉）。


## 第三批：设置里的 radio 换成绿色 + 全仓库 md 叠免责声明

- **`ui::radioRow` 样式与「切换歌手」面板的 radio 对齐**（用户要求）：
  原来是粉点（`kCheckPink`）+ 灰描边 + 0.46R 内点；现在照 `SongSelect.cpp` 里
  手机面板那套画 —— **底下一层柔和投影**（`IM_COL32(126,126,156,70)`，偏移 `+2*s`、
  半径 `*1.02`）→ 白色圆盘（hover 时混 `234,235,246`）→ **薄荷内点** `kPrimary`，
  半径 `0.72 * dotR * t`，`t` 走原来的 `animValue` 补间。**描边整个去掉**（参考里也没有）。
  `dotR` 保持 `11*s` 没动：设置卡片纵向余量很紧（`CPSEKAI_UI_TRACE` 那条），
  放大半径会让两行 radio 多吃 ~15px，先不为观感冒这个险。
  无头截图核对：`--settings --settings-tab 2` → `build/shots/ui_judge.png`，
  裁放大看 `crop_radio.png`（宽/标准 那一行）确认是白盘 + 绿点。**0 警告 0 报错**。
- **CREDITS.md 复审计（2026-09-24）+ 修正**：09-18 那版的数字被 09-19 素材瘦身打成废纸，
  这次全部 `git ls-files` 重测：`assets/` **255 文件 / 9.6MB**（mmw 214 = overlay 206 +
  顶层 6 + ui 2）、`Drafts/` 64、`docs/` 5、`assets/se/` 21、`assets/ost/` 1、`assets/fx/` 3、
  `assets/select/` 15。**已删**：`mmw/effects`、`mmw/sound`、`mmw/font`、`overlay/ap.mp4`、
  `overlay/ap-native/`、`ost/キミだけの跳躍.mp3`（09-24 grep 确认源码零引用）。
  `music-aliases.json`（185KB）之前整张表都漏登，已补进第六节。
  §八 的 `assets/test.sus` 其实在 `charts/` 里（gitignore 内），已改。
  nlohmann 那行的「两份实体」改成了「vendor 是一行转发头」。
- **免责声明「叠满」**：`AGENTS.md` / `CHARTS.md` / `CLI.md` / `CODE-REVIEW.md` /
  `COPYRIGHT.md` / `CREDITS.md` / `SETUP.md` 每个都加了**顶部横幅 + 底部整节**；
  **README.md 按用户要求没动**（它本来就有「关于」那节）。COPYRIGHT.md 新增「九、免责声明
  （完整版）」6 条（含「联系即删」），§八 贴 Release 用的那段措辞也改了 —— 原来写
  「本仓库不含任何官方游戏素材」是**错的**（assets 就在库里），改成「本发布包不含」。
  全仓库 md 只有根目录这 8 个（`git ls-files '*.md'` 核实）。

## 第四批（咨询，未改代码）：打算发 B站 + 带 assets 分发，问会不会被国服代理告

- **他问这个 = 他倾向这么做**。国服《初音未来:缤纷舞台》运营方核实过：
  **北京朝夕光年（字节跳动旗下）**，出版单位北京中电电子出版社，2025-03-27 公测。
- 查到一条**关键证据**：SEGA / Colorful Palette / Crypton 有公开的
  **《二次创作相关规范》**（台服官网 tw-pjsekai.com，2021 制定、2025-03-12 最后修订）：
  允许**个人或非法人团体的非盈利目的**二次创作（插画、同人本、模型、cos、**录屏截图介绍游戏**），
  并**明确禁止**「将音档以供人收听为目的提取上传」、禁止发布游戏影像作品本身、
  禁止暗示获得官方赞助/许可。
  → **这份规范一个字都没提「可以再分发游戏资源」**。所以「带 assets 发包」不在官方默许圈内，
  而「发录屏视频介绍」是明文允许的。这是给他建议的核心依据。
- 给的建议（三档，未替他决定）：A 只发源码/`--no-assets` + 视频演示（最稳）；
  B 剥掉国服独占（11001+）再发；C 真要带 assets：非营利、别挂广告赞赏、别用官方 logo，
  简介写清版权与 24h 删除。
- 判断口径（下次他再问同类问题可直接复用）：**「会不会被告」和「会不会被投诉下架」是两件事**。
  国内厂商真正动用刑事/大额索赔的是「未公开内容 + 大流量 + 牟利」那类
  （米哈游 2026 判例、和平精英「小胖解说」判赔 100 万）；对**非营利个人**，
  执业律师的公开口径是「发警告函、通知删除即可，不宜过度追责」。
  所以起诉概率极低，**平台投诉下架的概率才是真风险**（B站稿件 + 网盘链）。
- 另：国服素材来源 `storage.sekai.best/sekai-cn-assets` 是第三方镜像（解包产物），
  带本地化痕迹、一眼能认，是最容易被投诉的那部分。

## 第五批（咨询，未改代码）：chartdl 的法律风险 + 「藏功能规避风险」可行吗

- **chartdl 实测体检**（`downloader/chartdl.cpp`，3425 行）：
  - **纯 WinHTTP 的 HTTPS 客户端**，`grep` 过 aes/decrypt/加密/hmac/sign/token = **零命中**
    → 不解密、不绕技术措施、不碰官方账号，只有 `kSecure = WINHTTP_FLAG_SECURE`。
  - 内置三个数据源：`assets.unipjsk.com/startapp/music/{music_score,jacket}/`、
    `/ondemand/music/long/`、以及 `storage.sekai.best/sekai-cn-assets`（国服独占，id>=10000）。
  - 选项只有 `--download <ids> / --diffs / --vocals / --out / --list` 等，**没有「全曲库一键」**
    （必须给 id 或 GUI 勾选）。
  - `package.sh:43` **把 chartdl.exe 打进发布包**，CLI.md 里有公开用法。
- **给他的结论**：
  1. 工具本身干净 —— 不构成直接侵权，风险档位是「帮助/教唆」这一档（《民法典》1169），
     比"直接分发素材"低一档，但比什么都不做高。
  2. 让风险偏高的两点：**内置了镜像地址**（不是让用户自己填）+ **把国服独占那批也纳进来**。
  3. **「故意藏功能需要特殊方式启动」= 无效且有害**：法律判断的是实际具备的能力不是入口深度；
     藏起来反而在「故意/明知」这个要件上**加重**（主观恶意证据）；国内平台也把"规避审核"
     当加重情节；还挡不住举报的人（举报者不翻隐藏参数）；最后对自己也有害
     （违反 AGENTS.md 立的"技术细节必须写文档"规矩，下次改不动）。
     区分标准是**目的**：藏"不想让审核看到的能力"= 规避；藏"普通用户用不上的调试开关"= 工程惯例。
  4. 真正降风险（跟隐藏正相反，是减少实际能力并公开）：
     A 数据源改用户自填（`--source` / `chartdl.json`，默认一个都不带，学 yt-dlp）；
     B 砍掉国服那一路（`id >= 10000` → `storage.sekai.best`），把自己从"国内代理的钱袋"里摘出去；
     C 公开的 opt-in 确认可以留，但它的价值是留"使用者主动选择"的痕迹，不是免责；
     D **不预打包/不缓存/不托管/不做全曲库合集** —— 这条比上面都管用。

## 第六批（咨询，未改代码）：「上游也直接分发官方 asset，为什么没事」

- 他把上游当**参照先例**（意思是"上游都没事，我为什么要怕"）。我把上游实测了一遍
  （`gh api watagashi-uni/sekai-mmw-preview-web`），结论是**那个类比不成立**：
  - **上游真在公开仓库里托管官方素材**：`public/assets/**` 共 **415 个文件**（`assets/mmw/`
    下有 overlay / effects / font / notes*.png / longNoteLine*.png / default.png 等）；
    仓库 **AGPL-3.0**，2026-03-14 建仓，**12 stars / 2 forks**，homepage
    `chartview.unipjsk.com` —— **和 `assets.unipjsk.com` 素材镜像站是同一组织**。
  - **上游 README（237 行）里 `非官方/免责/官方/侵权/删除/自担/自负` 全部零命中** ——
    只有「致谢与许可证」，里面列的是 `crash5band/MikuMikuWorld (MIT)` 与
    `TootieJin/pjsekai-overlay-APPEND (AGPL)`。**它对那 415 个官方 PNG 一个字都没交代。**
  - ⚠ **重要发现：overlay 那批图来自 `pjsekai-overlay-APPEND`，上游把它当"代码依赖"致谢**。
    也就是说 CppSekai 的 overlay 素材链是
    `pjsekai-overlay-APPEND (AGPL) → sekai-mmw-preview-web (AGPL) → 本仓库`。
    **这条链上每一环的许可都只能覆盖代码，不能覆盖 SEGA 的美术**
    （无权利者不能授权利 / nemo dat）。**git 历史里的 MIT/AGPL 标签不是护身符**，
    它只让素材"看起来有出处"。CREDITS.md §一 现在只写了 mmw_preview.cpp 的来源，
    没写「上游同时托管了 415 个官方素材文件」这条 —— **以后想补可以补**（他没要求，未动）。
- **给他的答复口径**（可复用）：
  - 「没事」要拆成「没被告」和「没人管」——前者是事实，后者是错觉；
    权利人**选择性执法**是常态，默许 ≠ 合法、≠ 授权。
  - 上游暴露面小的三个可验证维度：① 12 stars 的小圈子工具，无宣传、无"自带全素材"卖点；
    ② **形态是网页**（素材按需请求缓存，用于"在浏览器里看谱面"），
    不是可离线再分发的 zip/exe —— 前者接近官方规范允许的"介绍游戏"，后者是复制发行；
    ③ 它**自己就是素材镜像站的前端**，风险本来就在那个站上，不在仓库。
  - **这套处境不能平移给他**：他发 B站（境内平台 + 国服代理是境内公司 + 现成投诉通道 +
    B站响应极快）、他发的是**离线包**、他可能带国服独占素材。三个维度都比上游显眼。
  - 反而他现在**比上游做得多**（COPYRIGHT.md / CREDITS.md / 满屏免责声明）——
    但**声明不能免责，没声明也不能说明安全**，别把上游的"沉默"当成风向标。

## 第七批（重大发现，未改代码）：那批 repo 消失的真正原因 —— 官方 2026-05-05 起执行「资格罚」

> ⚠️ **这条改变了本项目的整个风险前提，以后所有版权相关的判断都要按新口径来。**

- **官方公告**（@pj_sekai，**2026-04-27 11:41**，196 万浏览 / 1227 转 / 6367 赞）原文：
  > 【お知らせ】5月5日以降に、SNS等で**外部非公式アプリの利用が確認された場合は
  > 公式大会・公式イベントへの参加禁止などのペナルティ措置**を実施いたします。
  - **执法工具是「资格罚」（禁赛 / 禁参加官方活动），不是诉讼、也不是 DMCA。**
  - 触发条件是「**在 SNS 等平台被确认使用外部非公式应用**」—— 也就是**发视频/发帖本身就是暴露行为**。
  - 链接：`https://x.com/pj_sekai/status/2048729214900461995`
- **直接后果（第一手证据）**：`sevenc-nanashi`（Chart Cyanvas / MMW4CC 作者）在
  `https://cc.sevenc7c.com/`（= 一个 gist：`gist.github.com/sevenc-nanashi/5f7616eb9655cf6ec4ae84e9ed4d036b`）
  公布：官方发声明要求**尽量删除既有视频**，于是他 **2026/05/04 JST（生效前一天）**
  把 14 个仓库转私有/删除 —— Chart Cyanvas 源码与归档、Potato Leaves、**MikuMikuWorld for
  Chart Cyanvas(MMW4CC)**、sonolus-pjsekai-* 系列、pjsekai-overlay、pjsekai-soundgen 系列、
  pjsekai-bg-generator 等。原话：
  > "**I DON'T WANT TO BE SUED.** (Yeah, I don't think they actually sue me, but I want to
  > apply my song to Sekai Next so I don't want to be blacklisted from official...)"
  > "**これで公式を責めるのはやめてください。** 創作譜面はいつでも潰される覚悟でやるものなのでね。"
  → **作者是主动撤的，动机是「保住参加官方活动的资格」**，不是被打掉。
- **反向核对**：`github/dmca` 里搜 `MikuMikuWorld` / `Project Sekai` / `pjsk` / `proseka` /
  `Colorful Palette` = **全部 0 条**；`sega` 19 条全是无关（Sonic 之类）。
  → **确实没有走 GitHub DMCA。** 记牢：**「repo 变 404」≠「被打了」** ——
  删除 / 转私有 / 改名，API 一律返回 404，**不能从外部归因**。
- **`crash5band/MikuMikuWorld` 实测 404**：但作者账号健在（名下另外 11 个仓库都在，
  `Glitter` 2026-04-16 还在推）→ **不是封号**，只是那个仓库没了。时间线吻合
  （v4.0 发布 2025-10-24，政策 2026-05 落地），社区归档 `Sono-Suite/MikuMikuWorld-Archive`
  的措辞是「if the original owner ends up **reviving** the repo」→ 指向作者自己撤。
  **但无直接证据，不能替他断定。** 存活副本：`Eyelove3rd/MikuMikuWorld`（2022 快照，
  非 fork）、`510208/MikuMikuWorld`（fork，2026-02 还在改中译）。
- **国服侧**：查到《初音未来:缤纷舞台》有《游戏用户条约》（5.1.6 禁止漏洞牟利 / 5.4.14 禁止数据异常）
  且**有实际封号公示**（活动漏洞那批）；JP 侧另有 `pjsekai.sega.jp/guideline`
  《禁止行為に関するガイドライン》（2022-04-05 制定，**2025-07-08 / 2025-11-28 改訂**，
  含 BOT/マクロ/外部ツール自動プレイ）。**没查到国服针对「站外第三方客户端」的专门公告**，
  但代理商立场不会更宽松。
- **❗必须向用户修正的一点**：我前两轮引的《二次创作相關規範》（2021 制定 / 2025-03-12 最后修订）
  是**旧口径**，我拿它说「录屏介绍游戏 = 允许」。**2026-05-05 之后官方已收紧**，
  何况新公告明确点了「**外部非公式アプリ**」——CppSekai 正好是这个类别里最标准的形态
  （比 chart previewer 更直接，它是玩家端）。**旧文档不能当护身符。**
- 我给的风险优先级改成：**① 资格罚（禁赛/禁参官方活动，几乎必然触发，因为触发点就是"发出来"）
  → ② 平台下架/删链 → ③ 诉讼索赔（仍然极低）**。
  并明确反对「伪装/换名发」这种规避（跟上一条口径一致：要么不发，要么接受）。
- 生态结论：官方一表态，能活下来的只剩「**不依赖官方素材**」的项目（MajdataPlay 那种
  仓库零官方素材、用户自己丢皮肤）—— 这次事件等于给那个方向盖了章。
- **待办（他没要求，未动）**：COPYRIGHT.md 第六节和第四节该补这条政策变化 + 资格罚；
  CHARTS.md / AGENTS.md 的「官方默许」措辞也该跟着改。下次他点头就做。

## 第八批：写了宣传用语备选（`docs/promo.md`，新建）

- 他的需求：**宣传用语 + slogan，侧重「用学校希沃 + 和同学玩音游」**（要发 B站）。
- 产出 `docs/promo.md`，十节：主 slogan 候选（5 条）/ 备选按角度分组（场景·反叛·电教委员·务实）/
  B站标题 / 简介长短两版 / 封面大字 / 置顶评论 / 开头 10 秒口播 / 项目一句话简介 / 标签 / 取舍建议。
- 我自己推的主 slogan：**「上课是教具，下课是街机。」**
- 摸到的两条写作经验（下次可直接用）：
  ① **「电教委员」这条叙事线是他独有、别人抄不走的**，写进去了（「六年电教委员，就为了这几天课间」）；
  ② **「希沃 + 课间十分钟」不需要提游戏名就成立**，而且更破圈 —— 因为它讲的是所有学生的共同经验。
     所以建议标题/封面先留白，别一上来就堆 `#世界计划`（这条同时也是规避 IP 名的自然理由，不必说教）。
- 没往 README 加任何东西（他自己立的规矩：技术细节禁止往 README 加；这次是宣传文案，也一并放 docs/）。
