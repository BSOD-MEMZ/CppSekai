# 2026-09-07

- 完成环境配置：setup.sh 因沙箱/删除保护多次失败，改为手动分步执行——zig 0.14.1 zip 用 Windows bsdtar 解压（Git Bash tar 不认 zip、Expand-Archive -Force 被安全删除钩子拦截，且需先 rm -rf 不完整目录再解压）；SDL2 2.32.10、upstream 资源、charts(0075/0127) 手动下载安装。
- bash build.sh 构建成功（build/cppsekai.exe，仅 DirectXMath 弃用警告）；截图模式验证通过（build/shot.png 正常渲染 pjsk 舞台）。
- 教训：本机环境下 D 盘大量写操作需关沙箱；删除保护钩子会拦 Expand-Archive 覆盖和大批量 rm，解压前先手动清目录。

## Intro 1:1 还原（第二轮）
- game/Intro.{hpp,cpp} 重写为上游 drawOpeningIntroOverlay/introCardAlpha/openingPlayfieldVisibility 的逐行移植：
  固定 4s 卡片（kHudIntroDurationSec）、退出淡出 0.56s、遮罩 (104,104,156,0.82)、双波 start_grad
  （INTRO_GRAD_START_SEC=1.0、周期 2s、easeOutQuad）、封面 350px@(148,底部104)、难度色块
  （APPEND 渐变）、标题字距 5px、metaColor 纯白、字体 42/38/20（DB/EB|Noto）。
- Renderer::renderFrame 增加 playfieldVisibility：背景常显、舞台和 note alpha 乘 visibility
  （卡片期间全黑→4s 后 1.8s 淡入）；main.cpp leadIn 默认 6.0 且 >= kMinLeadInSec(5.8)。
- main.cpp 新增 --title/--lyricist/--composer/--arranger/--vocal/--difficulty（对齐上游 CLI）；
  description1 = "作詞：…　作曲：…　編曲：…"，description2 = "Vo. …"。
- 坑：MinGW argv 是 ANSI 代码页，日文参数会变 '?' —— main.cpp 用 CommandLineToArgvW +
  WideCharToMultiByte(CP_UTF8) 重取参数（需 windows.h+shellapi.h）。
- 坑：ImGui 1.92.5 字形检查要用 font->GetFontBaked(size)->FindGlyphNoFallback，ImFont 上没有。
- 三个字体实际都是 OTTO/CFF，但 stb_truetype 能读且字形齐全（Rodin EB 可直接加载）。
- 选歌难度：unipjsk SUS 头固定写 "#DIFFICULTY 0"，改为文件名推断优先。

## 真实元数据接入（第三轮）
- 封面 CDN 规律：assets.unipjsk.com/startapp/music/jacket/<bundle>/<bundle>.png，
  bundle 是 jacket_s_075（3位 id，不是 0075）；master 数据来自
  viewer-api.unipjsk.com/api/master/<dataVersion>/musics|musicVocals|gameCharacters。
- 已下载 charts/0075.png、0127.png（官方 jacket）。
- 新增 sidecar JSON：`<谱面名>.json`（title/artist/lyricist/composer/arranger/vocal/difficulty/level），
  SongSelect 用 nlohmann 解析并填入 ChartEntry（新增 lyricist/composer/arranger/vocal 字段），
  main.cpp startSession 用它们兜底 intro 卡的 作詞/作曲/編曲/Vo.。
- 已生成 0075_master.json（ウミユリ海底譚 / n-buna / 初音ミク）、
  0127_master.json（トンデモワンダーズ / sasakure.UK / 初音ミク、KAITO）。

## pjsk UI 组件库 + 暂停流程（第四轮）
- 新增 `game/Ui.{hpp,cpp}`：beginCard/endCard（圆角浅灰卡片+暗背板+关闭X）、capsuleButton（胶囊按钮 白/薄荷）、caption、messageDialog。close.webp→assets/mmw/ui/close.png（ffmpeg -frames:v 1，webp 单帧转 png 需此参数），hud key `ui_close`。
- 调试设置面板改 pjsk 卡片样式；暂停流程：血条右端 600x600 点击区（或空格）→暂停对话框 重试/放弃/继续演出；`--show-pause-dialog` 供无头验证。
- 血条 v3：2560x600 画成 640x150 虚拟坐标，fill 胶囊 u∈[0.148,0.793] 右→左排空，<=30% 用 danger.png；`game::lifePauseRect()`。
- MISS 不显示根因：registerMiss 不写 lastJudgeTimeSec → HUD 判定字不刷新；改为 registerMiss(songTimeSec)。
- 字体坑：Intro 字体日文码位缺简体字（设置/关闭显示"?"），扩展 22 个码位 + NotoSansCJKSC-Black MergeMode 并入 Rodin body。
- 分层：HUD→background drawlist，UI 卡片→窗口层，对话框才能压暗 HUD（foreground 会被 HUD 压、窗口层内容会被卡片盖）。
