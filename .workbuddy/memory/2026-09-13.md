# 2026-09-13

## hold 尾 flick 触摸屏必须松手才能滑 + 全屏开屏黑底（eb21a96）

用户报两件事：① hold 尾的红色 flick 在触摸屏上很难触发，必须"松手再滑"；黄色稍微好点。
官方是按住不放直接滑就该清。② 全屏模式 + 图片开屏，下次启动会"黑底 + 图片"，
希望无论什么模式，开屏都只是透明底的一张图。

### ① 根因（判定引擎本身没问题，全在 main.cpp 输入层）

先确认引擎侧：hold 尾（kind 2 + `holdTail`）在 `findCandidate` 里是放行的
（`holdTail && !(wantFlick && kind==2)` 那条），所以问题在"喂进去的 lanePos/手势"。

把核心的 HitEvent dump 出来（`hitEvent[...]` 那 8 行日志）看到关键结构 —— **走位 hold**：

```
hitEvent[4] t=5.455 center=1.50 width=3.00 kind=0     ← hold 起点 tap
hitEvent[5] t=5.455 center=1.50 width=3.00 kind=5 end=5.909   ← kind 5 标记（起点轨道 + 终点时间）
hitEvent[6] t=5.682 center=1.50 width=3.00 kind=4      ← tick
hitEvent[7] t=5.909 center=4.50 width=3.00 kind=2      ← **尾判 flick，轨道是终点轨道！**
```

三个原因，全部修掉：

1. **尾判按"按下时的轨道"判定**（主因）。`movePointer` 一直拿 `track.lanePos`
   （FINGERDOWN 时算的）去 `judgement.flick()`。`laneCovers` 要求落在音符中心
   ±(宽度/2+margin)，走位 hold 起步 1.50、尾巴 4.50 → 覆盖范围 [2.2, 6.8]，1.5 落空
   → 按住怎么滑都是 None。**松手再滑能用，是因为新触点从当前手指位置重算了轨道。**
   新增 `flickJudge()`：先试 `lanePos`，None 再试 `restLanePos`。
   `restLanePos` = 手指**低速**时记录的轨道（跟随走位会刷新；滑动过程中速度高不刷新，
   所以不会被"抬头透视"把 lane 拉偏：`lane = worldX/worldY`，手指抬高水平位移会让
   同一屏幕 x 映射到更小的 lane）。
2. **采样周期被静止空档稀释**。触摸屏手指不动时**一个 motion 事件都不发**，所以 flick
   第一帧的 `dt = now - lastMoveTimeSec` 可能是几百毫秒 → 800px/s 被算成 ~100px/s → 刷不出来。
   `dt` 上限钳到 `kFlickMaxSampleSec = 0.03`；空档 > 0.05s 视为"手指停着"→ 清空速度滤波。
3. **一次触点只能 fire 一次 flick 的闩锁**。`track.flicked` 一旦置位，本次触点后面的滑动
   全被无视；hold 途中任何提前/误触的滑动都会把机会用掉。改成可重复触发：
   fire 后把 `travelUp/Side` 归零（同一次连续滑动不会每个事件都触发）+ 60ms 冷却
   （`kFlickRefireSec`）；FINGERUP 的 last-chance 也不再因"已触发过"而跳过。
4. 顺手：`track.isTouch` 显式记录触屏/鼠标（原代码用 `track.fingerId > 0` 猜，
   猜错的话阈值就从触摸的 500px/s 变成鼠标的 900px/s）；FINGERUP 原来硬编码 `true`。

**验证（可复现，不需要真手指）**：`--test-hits` 会把每条 HitEvent 按自己的
`event[1]`（音符轨道）喂给判定引擎，正好可以模拟"滑在正确轨道上"。把那一行的
lane 临时改成 1.5f 就复现旧行为：

```
尾判轨道 4.50 滑 → perfect=11 miss=0
按下轨道 1.50 滑 → perfect=10 miss=1   ← 旧行为，复现漏判
```

回归：main.cpp 改动前后 `--test-hits` 整曲统计**逐字节一致**
（perfect=303 miss=8 tails=5 score=457238；那 8 个 miss 是既有问题，不是回归）；
`--auto` 整曲 0628 HARD → perfect=738 miss=0。

**遗留（未修，用户可能在意）**：走位 hold 的尾巴在 `Judgement.cpp::load()` 里
**不会被打上 `holdTail` 标记**（flagging 用 `candidate.center` vs 标记 center 匹配，
走位时两者不同），所以 `hold.tailIndex` 解析不到 → 它按普通 flick 音符走
（滑到就清、按住不放会 180ms 后 miss），`tails` 统计少算它，松手分级那条路也不生效。
行为上可接受（官方 flick 尾本来就要滑），要改成"按时间窗匹配"才能彻底对齐。
同一处还有已知的 O(n²) 扫描（每根 hold 都遍历全部音符）。

### ② 全屏开屏黑底

图片开屏的透明是靠 `SDL_GL_ALPHA_SIZE=8` + `glClearColor(0,0,0,0)` +
`DwmExtendFrameIntoClientArea(-1,-1,-1,-1)` 实现的（SDL2 没有 SDL_WINDOW_TRANSPARENT，
那是 SDL3）。但窗口一创建就 `SDL_SetWindowFullscreen(FULLSCREEN_DESKTOP)` →
**覆盖整个桌面的窗口被 Windows 的 fullscreen optimizations 接管，DWM 合成被绕过，
逐像素透明失效** → alpha=0 变成不透明黑。

修法：图片开屏期间**一律不进全屏**，用普通无边框窗口（就是窗口模式那套），
加载完在 boot 末尾再应用用户设置的模式（那里原本只做 windowed 的 `SetWindowBordered`）。
另外把开屏窗口尺寸限制在 `SDL_GetDisplayUsableBounds - 16px` 内（万一存档分辨率正好
等于显示器尺寸就会被 FSO 抓走）—— 开屏窗口除了居中那张图整体都透明，缩几像素肉眼不可见。
经典开屏（splashStyle=1，深色底）保持"创建即全屏"不变。

验证：`--window fullscreen` 截图 1920x1080、`--window windowed` 截图 1366x768
（存档里的分辨率），证明全屏是延后生效且 resize 事件把 renderer/core 尺寸都带上了。
顺手把 F 键的 `fullscreen` 状态改成 `SDL_GetWindowFlags()` 查真实状态 ——
以前在设置里切成全屏后首次按 F 会因为本地标志是 false 而"没反应"。

## ② SMTC 一直显示"暂停" + ③ 选曲壁纸背景 / 索引面板 / 过渡（45b1c30、83e40fb）

### SMTC：MediaPlaybackStatus 数值整组偏一

用户报"软件还是无法汇报 SMTC 信息"。抓 `[media]` 日志一看：`get_DisplayUpdater`/
`put_Title`/`Update`/`UpdateTimelineProperties` 全是 hr=0，IsEnabled 回读也是 1，
**所有调用都成功**——问题在枚举值：

```
真值(官方枚举文档)：Closed=0 Changing=1 Stopped=2 Playing=3 Paused=4
代码里写的是：      Stopped=3 Playing=4 Paused=5   ← 整组 +1
```

于是 `put_PlaybackStatus(Playing)` 实际发的是 4 = **PAUSED**，播放中以"暂停"上报，
时间轴却还在走（Paused=5 更是越界）。改成 2/3/4。

顺手把这条链路做成可自检的（以前 `setTrack` 一个字都不打，"元数据失败"和"shell 无视我们"
长得一模一样）：setTrack 打印每个 HRESULT + HSTRING 是否构造成功，写完 Title 立刻 get_Title
回读（打长度 + 首码点，MinGW 的 printf 打 `%ls` 会串行 → 别用）；updatePlayback 里加
PlaybackStatus 的写入/回读对照。实测 Title readback = 6 chars 首码点 U+30E2（モ）、
PlaybackStatus readback = 3 (wrote 3) ✓。

ABI 用 `.workbuddy/tools/winmd_dump.py` 核对过，**这三处代码本来就对**：
`ISystemMediaTransportControls`（30 个方法；注意 `get_SoundLevel` 在索引 3、**Stop 在 Pause 之前**）、
`ISystemMediaTransportControlsDisplayUpdater`（Thumbnail 4/5、MusicProperties 6）、
`IMusicDisplayProperties`（Title 0/1、AlbumArtist 2/3、Artist 4/5）。
工具本身修了两个 bug：方法范围原来按"匹配到的类型"排序 → 每个接口都打印到方法表末尾
（现在按全部 typedef 排序，范围才是真槽位数）；加 `~name` 子串匹配（versioned 接口名难猜）。
**枚举数值故意不导出**：winmd 的 Constant 表列顺序和 ECMA 不一致，我试出的读法
（[Value u32][Type u16][Parent u16]）虽然能过"半个表都是 I4"的检查，但值会错配到相邻成员，
宁可不给（文档为准）。

**仍未做**：封面缩略图（put_Thumbnail）没设，浮层只有标题/艺术家没有专辑图。

### 选曲界面三件（详见 AGENTS.md「选曲界面」小节）

1. **背景可用桌面壁纸 + 模糊**：`bgStyle/bgBlur/bgDim` 三个设置项；路径三级回退
   （SPI_GETDESKWALLPAPER → 注册表 → `%APPDATA%\...\Themes\TranscodedWallpaper`，后者是个
   没扩展名的 JPEG，stb_image 按内容嗅探所以能读）；解码/降采样到 1024/三次 box 模糊都在
   `Renderer::loadBackdropTexture()`，**只在设置开着时加载**（默认零启动开销）。
2. **点段标题 → 字母索引面板**：分组开着时段标题可点，列表变成 key 网格（当前段高亮），
   点 key 飞过去。为配合用户举的例子「& # A B C D E F」加了第 4 种分组模式**按首字母**
   （A-Z / 0-9 / あ い う…）。**排序/分组也持久化了**（以前每次启动回默认）。
3. **过渡**：手机面板入场从右滑入 + 淡入（挂在既有的"手机顶点整体旋转"那趟循环里）、
   索引面板缩放淡入、选中卡片行高按槽位指数趋近。

### 新增的验证手段（很重要，以后 UI 改动都该这么验）

模型**看不了图**，所以 UI 改动一律走"像素断言 + 输入注入"：

- **`.workbuddy/tools/winsend.c`** → `build/winsend.exe`（`zig cc -O2`）：按标题找窗口，
  `PostMessage` 送 click/move/key，可以在 `--screenshot` 跑着的时候驱动 UI。
- **断言例子**：背景区均值 110,113,127（默认渐变是 26,23,52）→ 画的是壁纸；
  blur 0.0/1.0 同区域方差 1703.6 → 796.9 → 模糊生效；点标题后日志
  `[select] section index opened`、面板底板像素正好 22,20,44、高亮色 126,118,208
  各上千像素 → 面板画出来了；点第 6 个 key → `[select] jump to section 'ら'` 且列表区域
  哈希变化 → 真滚过去了；入场动画把时长临时改成 3s 抓中间帧，手机区平均差 80.7 / 列表 0.6
  → 只有手机在动（抓完改回 0.38s）。
- 坑：这个 bash **没有 `sleep`**（用 `python -c "import time;time.sleep(4)"`）；后台起进程后
  `cppsekai.log` 一时删不掉（句柄还占着，先 `mv`）；`--screenshot-time` 在选曲界面最小 0.5s。
- 回归：`--test-hits` 整曲统计与改动前**逐字节一致**（perfect=303 miss=8 score=457238），
  `--auto` 整曲 perfect=213 miss=0。测试时改过 userdata.json（bgStyle/groupMode），
  跑完已从 `userdata.json.bak` 还原（那个文件没被 git 跟踪，是用户的真实存档）。

## ④ 失血阴影没绕满四周 / SMTC 封面 / 段索引改纯字母（0032dba）

用户反馈四条（含"你不是支持多模态吗"）：失血阴影不在窗口四周；SMTC 能传但传不了封面；
段索引弹紫窗口太割裂，要移除多余元素只留首字母、点空白恢复、字母加聚拢/散开动画；以及质疑我不能看图。

### 失血阴影（`main.cpp` 的 damageVignette/deadVignette）

原来的四边带为了"避免角上叠加"内缩成：左右带全高、上下带只占中段 → **角上只有横向渐变、
没有上下方向的压暗**，顶/底边在角附近是亮的 → "阴影没绕窗口一圈"。改成四条边带内缩（互不
重叠）+ **四个角方块四色插值**（外角两侧暗、内角 0）。

验证手法（这次最有价值的部分）：**A/B 两张图逐像素比**——一张 life=0（dead vignette 常亮，
alpha=72 → 期望压暗系数 0.718），一张 `--auto`（无阴影），同一坐标相除就能把 alpha 单独
解出来（直接比较不同位置是错的，内容差异会淹没效果：我第一次测就得到 1.391 这种鬼数）。
结果：六处边缘（含两个上角、两个左角/左边中）全部 0.72~0.74 ✓，角区从旧几何的 ~0.96 变成 0.733。

### SMTC 封面（`put_Thumbnail`）

文档推荐的 `StorageFile.GetFileFromPathAsync` **在本进程永远不完成**：线程是 STA，async
完成回调进 apartment 队列，轮询 `get_Status`（加不加消息泵都试过）一直停在 Started。
改用同步路线 `Windows.Foundation.Uri` + `RandomAccessStreamReference.CreateFromUri` ✓
（shell 自己解析 file:// URI）。路径要绝对化 + 百分号编码。实测 `put_Thumbnail hr=0`，
URI 回读 `file:///D:/Dev/CppSekai/charts/0628.png`。
顺带修 `winmd_guid.py`：它内部按"命名空间.类型"存表，用短名查永远 NOT FOUND（以前只能手抄
IID）；现在短名后缀匹配 ✓。

### 段索引重构 + 三个点击坑

去掉了紫底板/标题/说明/关闭叉，只留字母网格（当前段白字 + 短横杠），点空白恢复。
字母用径向位移 + 淡出 + 逐字延迟做聚拢/散开；列表行在面板打开时按顶点 alpha 批量淡出
（63k → 646 亮像素）。

三个坑（都是自己挖的，改这块前必读 AGENTS.md 同名条目）：
1. **整块 InvisibleButton 当背景会抢 active id** → 后面提交的字母收不到点击（第一次"点字母
   没反应"的真凶）→ 手写命中测试。
2. **格子矩形要半开区间** → 边界像素被两个字母同时命中，一次点击跳两段（实测 y=573 时
   i=0 和 i=4 同帧都触发）。
3. **按下+抬起可能同帧** → 列表的 release-commit（点标题开面板）和面板的 press 处理会吃
   同一次点击 → 用 `indexOpenAtPress` 门控。

另外记住：`--screenshot-time` 在**选曲界面是"进入选曲后的秒数"**（不是程序启动时间），
所以要在动画中间帧抓图得按选曲时钟算；而且 winsend 点击要等 ~6.5s 才稳（启动约 2.4-2.9s
+ 选曲首帧就绪）。

### 关于"你不是支持多模态吗"

要跟用户讲清楚：多模态能力（ImageGen 等）和**读懂图片**是两件事，当前这个会话背后的模型
（DeepSeek-V4 Flash）没有视觉输入，Read 图片工具会直接返回 "does not support reading images"。
所以 UI 改动我走的是"winsend 注入输入 + pngcrop 读像素做断言"这条路子（比肉眼更可复现）。
换一个带视觉的模型时可以再看图。
